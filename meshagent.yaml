kind: ServiceTemplate
version: v1
name: imagebot
variables:
- name: "system_prompt"
environment:
- name: SYSTEM_PROMPT
  value: "{system_prompt}"
ports:
- num: "*"
  endpoints:
  - path: /agent
    identity: imagebot
    type: meshagent.callable
