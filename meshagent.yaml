kind: ServiceTemplate
version: v1
name: codebot
room_storage_path: /data      # mount room storage path for the agent to write files to 
ports:
- num: "*"                    # automatically assign an available MESHAGENT_PORT for the agent to run on 
  liveness: "/"               # ensure the service is alive before connecting to the room
  endpoints:
  - path: /agent            # service path to call and run the agent on
    identity: codebot         # name of the agent as it shows up in the Room
    type: meshagent.callable  # instructs the room server to call this endpoint
